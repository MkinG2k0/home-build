/** Время (мс), в течение которого данные считаются актуальными без повторного запроса */
export const STALE_TIME_MS = 30 * 60 * 1000; // 30 минут

/** Время (мс) хранения неиспользуемых данных в кеше перед удалением (не меньше PERSIST_MAX_AGE_MS при персисте) */
export const GC_TIME_MS = 7 * 24 * 60 * 60 * 1000; // 7 дней

/** Ключ в localStorage для персистентного кеша запросов */
export const PERSIST_CACHE_KEY = "home-build-query-cache";

/** Время (мс) жизни персистентного кеша; после истечения при загрузке кеш игнорируется */
export const PERSIST_MAX_AGE_MS = 7 * 24 * 60 * 60 * 1000; // 7 дней
